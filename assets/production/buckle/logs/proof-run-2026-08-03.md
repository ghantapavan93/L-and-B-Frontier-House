# Buckle proof run — 2026-08-03

**Blender:** 5.2.0 LTS (`C:\Program Files\Blender Foundation\Blender 5.2\blender.exe`),
headless, Cycles on GPU (OPTIX), AgX view transform, base contrast.

**Authoritative script:** `scripts/build_buckle.py` — imports the canonical paths
from `source/canonical-paths.json` (verbatim snapshot of `src/ui/frontier-ignition.tsx`).
`scripts/build_and_proof.py` and `scripts/silhouette_check.mjs` are an earlier
session's scaffolding, never executed against this master; superseded.

## Commands (run from the repo root)

```
blender --background --factory-startup --python assets\production\buckle\scripts\build_buckle.py -- --stage build
blender --background --factory-startup assets\production\buckle\blender\lb-buckle-master.blend --python assets\production\buckle\scripts\build_buckle.py -- --stage proof --which front
  (repeat --which three_quarter | illuminated | aperture)
blender --background --factory-startup assets\production\buckle\blender\lb-buckle-master.blend --python assets\production\buckle\scripts\build_buckle.py -- --stage overlay
```

## Results

| Check | Result |
| :--- | :--- |
| Silhouette deviation vs canonical SVG | width −0.025%, height −0.085% (tolerance ±1%) — **pass** |
| Geometry | 42,839 polys (budget ≤120k) |
| Validation | clean — no forbidden names, no text objects, 8 named materials present |
| Proof renders | 4/4 + overlay, 1024×768, 128 samples denoised, ~6–9 s each |

## States in the master (frames, constant interpolation)

1 = S1 dormant · 10 = S2 illuminated · 20 = S3 aperture · 30 = S4 routing line.
Cameras: `cam_front_ortho`, `cam_hero_front`, `cam_hero_34`, `cam_macro`, `cam_aperture`.

## Known refinements deferred to the final master

- Monogram emission reads mint-white at the core; tune toward #7fe0d6 (strength or
  compositing bloom — never baked).
- Leather tooling is a procedural placeholder, not 8C's floral scrollwork (sculpt task).
- Silver ramp reads warm champagne under the 3200K key; deepen dormant ramp.
- N/S stones brush the stitch row (canonical coordinates preserved; clean in final).
- Chamfer is beveled approximation of 45° + 0.4 mm fillets; refine profile.
- Engraving channel uses displacement carve, not boolean; final master should
  boolean-cut for GLB export cleanliness.
- Back face is flat, not the specified 1.5% dome.
- Animation clips (`ignite`, `open`, `route_exit`) not yet baked — states only.
- No textures baked, no GLB exported — deliberately out of scope for the proof.
