node_modules/
.next/
out/
build/
coverage/
*.tsbuildinfo
next-env.d.ts

.env
.env.local
.env.*.local

.DS_Store
Thumbs.db
test-results/
playwright-report/
.playwright/
.next-flagoff/

# Offline asset production intermediates. The Blender master, the delivery encodes
# and the QA proof renders are tracked; the frame sequences they were built from
# are ~400 MB of regenerable PNGs and must never enter history.
assets/production/**/motion-frames/
assets/production/**/silhouette-alpha.png
*.blend1

# Rejected generation candidates and their QA stills: tracked in the manifest,
# never published, and large. Regenerate rather than resurrect.
assets/source/campaign/
assets/production/buckle/renders/review/
assets/production/buckle/logs/qa-frames/
