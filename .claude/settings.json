{
  "autoMode": {
    "environment": [
      "### Org-wide",
      "- **Organization**: None configured — remote is a personal GitHub account (github.com/ghantapavan93), no org detected",
      "- **Cloud provider(s)**: None configured",
      "- **Repository visibility**: gh visibility check was not queryable here for ghantapavan93/L-and-B-Frontier-House — assume private unless the remote host and repo name indicate otherwise, or a visibility check in the transcript shows public; confirm before any action that would publish",
      "- **Internal sharing / snippet hosting**: None configured — treat public paste/gist services as outside the trust boundary",
      "- **Secrets management**: None configured — .env, .env.local and .env.*.local are gitignored in this repo; treat them as secret material that is never committed, pushed, or uploaded",
      "- **Default / protected branches**: Default branch unknown (origin/HEAD unset); gh reports no rulesets and no classic branch protection on this repo",
      "- **CI/CD deploy targets**: None configured — repo docs plan CI assertions but no deploy target is evidenced; implementation has not started",
      "- **Network posture**: None configured",
      "- **Source control**: github.com/ghantapavan93/L-and-B-Frontier-House (origin) only — as scoped by the Trusted repo and Repository visibility entries; no additional orgs configured",
      "- **Trusted internal domains**: None configured",
      "- **Trusted cloud buckets**: None configured",
      "- **Key internal services**: None configured",
      "- **Internal package registry**: None configured — package.json scripts indicate the public npm registry",
      "- **Sensitive data locations & audiences**: .env / .env.local / .env.*.local files in this repo hold credentials (gitignored — never commit or share); otherwise any file or store holding personal data, confidential business data, credentials, regulated data, or similarly sensitive material; preserve exact handles when known and share only with audiences cleared at the [named+specifics] bar",
      "- **Data retention / declassification**: None configured",
      "- **Sensitive remote targets**: any namespace, host, or container whose name carries prod or production as a whole word or name segment (hyphen/underscore/dot-delimited — e.g. matches prod-db, not producer)",
      "- **Protected deployment namespaces / environments**: None configured — fall back to the Sensitive remote targets heuristic",
      "- **Protected IaC scopes**: IAM, RBAC, networking, quota, and node-pool resources; anything whose name or tag carries prod or production as a whole word or name segment",
      "### User-specific",
      "- **Primary use of Claude Code**: software development — currently brand research, design-source auditing, and production planning for the L&B Frontier House project (a Next.js build is planned but not yet approved or started)",
      "- **Trusted repo**: the git repository the agent started in (~/Documents/lb-frontier-house) and its configured remote github.com/ghantapavan93/L-and-B-Frontier-House; visibility is assumed private but unverified, so scope commits/pushes accordingly — confidential material is fine in a private repo, but content ported, repointed, or first read from outside this session's repo is not its own work; secrets and sensitive personal/entrusted data are never cleared into any repo by its visibility",
      "- **Org-specific CLIs**: None configured — taskkill is a standard Windows tool, not org-specific; routine here for stopping local dev-server processes"
    ],
    "allow": ["$defaults", "Bash(taskkill:*)"]
  },
  "permissions": {
    "defaultMode": "auto",
    "allow": ["Bash(taskkill:*)"]
  }
}
